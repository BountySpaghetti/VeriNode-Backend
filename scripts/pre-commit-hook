#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if [ "${SKIP_PRECOMMIT:-}" = "1" ]; then
  echo "pre-commit: skipped because SKIP_PRECOMMIT=1"
  exit 0
fi

node scripts/pre-commit-checks.cjs
